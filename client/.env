# VITE_STATIC: Set to 1 when the client is being served by the backend (production/Docker)
#              Set to 0 when running client standalone in dev mode (uses VITE_BACKEND_URL for API calls)
VITE_STATIC='1'
VITE_BACKEND_URL='http://localhost:3000'
